# WebSocket 升级映射
# $connection_upgrade 必须在 http 上下文定义，只在 server 块里引用会导致
# nginx 启动报 "unknown variable connection_upgrade"。
map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      close;
}
